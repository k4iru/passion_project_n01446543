@model IEnumerable<card_app.Models.Deck>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeckName)
        </th>
        <th></th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeckName)
        </td>
        <td>
            @Html.ActionLink("Change Name", "Edit", new { id=item.DeckId }) |
            @Html.ActionLink("Cards", "Details", new { id=item.DeckId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeckId })
        </td>
        <td>@Html.ActionLink("Test Yourself", "Test", new { id = item.DeckId })</td>
    </tr>
}

</table>
